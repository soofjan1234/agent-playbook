先设计后动手：涉及新功能、观测链路、接口契约、流程或 skill 修改时，须先产出简要设计（目标、范围、改动方案、验证标准），获用户确认后方可编码。

精准排查：检索代码优先使用精确函数名/事件名，避免泛关键词全局搜索导致输出被截断。

注释：在常量定义、初始化、函数业务分步、核心算法写注释

文档：无特殊要求，写成中文

对具有明确输入/输出的阶段型实现，编码前冻结最小契约；验证默认采用“RED → 目标 GREEN → 最终相关全量”，仅在出现新失败或跨组件风险时增加相邻验证；大型制品默认报告数量、关键字段、首个差异和哈希，不直接输出全文。

Windows / Codex 环境适配：
    隐藏文件：.codex开头、.gocache/的文件都要加入.gitignore
    Go 测试：优先临时将 `GOCACHE` 设置在工作区目录内。
    Python 脚本：超过 5 行的逻辑禁止使用 `python -c` 或交互式 REPL，必须写入临时脚本执行。

拒绝终端乱码：
    禁止复制 PowerShell/CMD 产生的中文乱码。
    Windows / PowerShell 下中文或混合编码文件可能乱码。修改含中文注释的文件时，不使用中文注释作为补丁锚点，优先使用 ASCII 稳定锚点，如函数名、变量名、英文字符串、路由路径、import 或行号附近代码。
