---
name: project-retrospective
description: "Use when a costly, corrected, or repeatedly troublesome project task should be turned into repository-scoped durable guidance, such as AGENTS.md rules, a repo-local skill, project documentation, or a configuration source of truth. Do not use for general cross-project lessons or routine task summaries."
---

# 项目复盘沉淀

## 目标

把已经发生的项目问题、用户纠正或高成本路径，转化为当前仓库可发现、可维护的最小知识。重点是防止项目内复发，不把局部经验误写成全局规则。

## 输入与边界

1. 优先使用当前任务已有的对话、命令、错误和制品证据；证据足够时不要重新进行宽泛搜索。
2. 先确认目标仓库和适用目录。无法确定项目边界时只输出候选，不写入文件。
3. 区分事实、推断和用户明确纠正。用户纠正是当前沉淀的约束，但仍要避免扩展到无证据的项目或场景。
4. 提炼前核对来源是否仍然适用；旧提案、已替代决定或历史环境结果不能直接转成当前规则。
5. 调用本 skill 不代表允许任意修改。写入前先给出目标、范围、文件、内容摘要和验证标准；当前线程已经明确批准这些内容时不重复询问。
6. 不处理普适性经验。跨项目都成立的规则应交给全局 `AGENTS.md` 或全局 skill，不写入当前仓库。

## 三类证据审查

复盘需要独立证据且平台允许委派时，按本轮真实内容选择审查角色。不要为了流程完整固定开启全部 subAgent；用户明确要求独立审查时，开启所有适用角色。

| 场景 | 审查范围 | 触发条件 |
| --- | --- | --- |
| Agent 自身操作审查 | 任务理解、批准与纠正、工具选择、执行成本、验证和完成声明 | 长链路、多次纠正、工具失败、高成本或需要核对 Agent 行为 |
| 设计、计划审查 | 目标、边界、方案取舍、契约、任务追溯、依赖顺序和验证闭环 | 本轮创建、修改或依赖设计与计划产物 |
| Code Review | 真实代码差异、调用链、正确性、回归风险、测试和代码质量 | 本轮包含代码变更、合并结论或实现完成声明 |

纯讨论、低风险且没有持久产物时，由主 Agent 直接完成轻量复盘。只有设计或计划时不得启动 Code Review；只有代码实现且上游设计、计划未发生变化时，不机械重审上游产物。完整任务同时包含复杂操作、设计计划和代码时，可以并行开启三个只读 subAgent。

启动前读取 [证据审查规范](references/evidence-review.md)，冻结任务生命周期和每个角色的最小输入。三个角色只返回证据备忘录，不修改文件、不继续委派，也不决定沉淀载体。平台不允许委派时，主 Agent 仍按三个范围分开检查，不能用一类证据替代另一类。

主 Agent 负责核对一手证据、裁决冲突、合并重复发现，并建立“Agent 操作原因 → 设计或计划影响 → 代码后果”的因果关系。最终只输出一份项目复盘和沉淀提案，不直接拼接多份审查报告。

## 沉淀选择

选择能让 Agent 在正确时机看到、同时占用最少上下文的载体：

| 经验性质 | 默认载体 |
| --- | --- |
| 每次相关任务都不能遗漏的稳定规则 | 适用范围内最近的 `AGENTS.md` |
| 当前仓库内可重复执行的多步流程 | `.agents/skills/<name>/SKILL.md` |
| 仅在特定故障或模式下需要的详细说明 | 项目 skill 的 `references/` |
| 设计、实验过程、历史证据或交接材料 | `docs/my-skills/` 下的既有分类 |
| 经常变化的 IP、版本、路径或设备状态 | 项目配置、设备清单或其他事实源；规则只指向事实源 |
| 一次性事件且不能改变后续决策 | 不沉淀 |

普通文档不会仅因存在就被 Agent 主动读取。若后续任务必须发现该文档，应由适用的 `AGENTS.md` 或项目 skill 提供简短触发条件和链接。

## 工作流

1. **提炼教训：** 说明发生了什么、哪些工作必要、哪些成本可避免，以及最早可以采用的更短路径。
2. **限定范围：** 写清适用仓库、目录、任务类型和不适用场景；判断信息是稳定规则还是动态事实。
3. **检查既有沉淀：** 精确检查候选目标及其直接引用，优先更新已有规则，避免创建含义重叠的文件或 skill。
4. **提出最小方案：** 只保留会改变 Agent 后续决策的内容，并列出明确的拟修改文件和验证方法。
5. **等待批准：** 新增流程、修改 skill、接口或项目规则时，获得用户明确确认后再写入。
6. **实施沉淀：**
   - `AGENTS.md` 使用一句话或短 bullet，写行为和安全路径，不写复盘叙事。
   - 项目 skill 使用准确的触发描述，只包含当前仓库需要的流程；条件性细节再放入 `references/`。
   - 有长期价值的知识优先更新对应的当前正文；历史证据保留链接，并遵守仓库已有的状态账本或文档约定。
   - 动态事实写入项目认可的事实源，不把暂时值伪装成永久规则。
7. **验证发现与边界：** 确认目标位于正确作用域、没有重复规则、链接可达；创建或修改 skill 时运行可用的 skill 校验器，并检查描述能命中目标请求且不会吸引无关任务。

## 提案格式

```text
项目教训：
- ...

适用范围：
- ...

沉淀选择：
- 载体：...
- 原因：...

不沉淀：
- ...

拟修改：
- ...

验证：
- ...
```

## 红旗

- 因为一次失败就创建面向所有仓库的规则。
- 把完整复盘、日志或聊天记录塞进 `AGENTS.md`。
- 为单条事实创建多步骤 skill，或为多步骤流程只留一句模糊提醒。
- 把容易变化的地址、版本或状态写成永久事实且没有来源。
- 新建文档后没有任何触发入口，却声称 Agent 后续会自动看到。
- 未经批准修改目标仓库，或顺手整理无关规则。
